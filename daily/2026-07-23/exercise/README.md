# Day 4 练习说明

```bash
pip install pytest   # 或 uv pip install pytest
python -m pytest -q
python main.py
```

今日重点：

1. `prompt_utils.py` — **已实现**，先读懂三个函数
2. `tests/test_prompt_utils.py` — **请你补至少 3 个测试**
