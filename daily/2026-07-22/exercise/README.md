# Day 3 练习说明

```bash
pip install pytest   # 或 uv pip install pytest
python -m pytest -q
python main.py
```

完成：

1. `config_io.py` — `load_json` / `save_json` / `load_agent_config` / `save_agent_config`
2. `models.py` 已提供，一般无需修改
