# Day 5 练习说明

```bash
python3 -m pytest -q
python3 main.py add "标题" "正文"
python3 main.py list
```

今日重点：

1. `note_store.py` — **请你实现** `Note` / `NoteStore`
2. `main.py` — argparse CLI 已搭好，一般不用改
3. 数据默认写到当前目录的 `notes.json`（可用 `--file` 指定）
